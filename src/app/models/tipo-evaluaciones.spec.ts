import { TipoEvaluacionesModel } from './tipo-evaluaciones';

describe('TipoEvaluaciones', () => {
  it('should create an instance', () => {
    expect(new TipoEvaluacionesModel()).toBeTruthy();
  });
});
